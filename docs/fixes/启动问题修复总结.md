# 启动问题修复总结

## 🔴 问题描述
启动后端服务时报错：
```
java.lang.IllegalArgumentException: Invalid value type for attribute 'factoryBeanObjectType': java.lang.String
```

## 🔍 问题原因
1. **MyBatis-Plus版本不兼容**: 使用的是 `3.5.5` 版本，与 Spring Boot 3.2.0 存在兼容性问题
2. **缺少@MapperScan注解**: 主启动类没有配置Mapper接口扫描路径，导致MyBatis-Plus无法正确初始化

## ✅ 解决方案

### 1. 升级MyBatis-Plus版本
**修改文件**: `backend/pom.xml`
```xml
<!-- 修改前 -->
<mybatis-plus.version>3.5.5</mybatis-plus.version>

<!-- 修改后 -->
<mybatis-plus.version>3.5.7</mybatis-plus.version>
```

### 2. 添加@MapperScan注解
**修改文件**: `backend/src/main/java/com/bank/workflow/WorkFlowApplication.java`

**修改前**:
```java
@SpringBootApplication(scanBasePackages = "com.bank.workflow")
public class WorkFlowApplication {
```

**修改后**:
```java
@SpringBootApplication(scanBasePackages = "com.bank.workflow")
@MapperScan("com.bank.workflow.infrastructure.persistence.mapper")
public class WorkFlowApplication {
```

### 3. 更新启动成功信息
同时更新了启动成功后的提示信息，端口改为9099：
```java
============================================
工作流系统启动成功！
Workflow System Started Successfully!
============================================
访问地址: http://localhost:9099
接口文档: http://localhost:9099/doc.html
Druid监控: http://localhost:9099/druid/
============================================
```

## 📋 修复步骤
1. ✅ 升级MyBatis-Plus从 3.5.5 → 3.5.7
2. ✅ 添加 `@MapperScan("com.bank.workflow.infrastructure.persistence.mapper")` 注解
3. ✅ 更新启动提示信息（端口9099）
4. ✅ 重新编译打包: `mvn clean package -DskipTests`
5. ⏳ **请重新启动后端服务**

## 🔧 版本兼容性说明

### Spring Boot 3.x 与 MyBatis-Plus 版本对应关系
| Spring Boot | MyBatis-Plus | 兼容性 |
|------------|--------------|--------|
| 3.2.0      | 3.5.5        | ❌ 有问题 |
| 3.2.0      | 3.5.7        | ✅ 完全兼容 |
| 3.2.0      | 3.5.8+       | ✅ 完全兼容 |

### 为什么需要@MapperScan？
- MyBatis-Plus需要知道Mapper接口在哪里
- 通过`@MapperScan`注解指定扫描路径
- Spring Boot会自动将Mapper接口注册为Bean
- 如果不配置，会导致Mapper接口无法被识别

## 🚀 下一步
**请在IDEA中重新启动后端服务，然后我们继续前端开发（选项A）！**

启动成功后您应该看到：
```
============================================
工作流系统启动成功！
Workflow System Started Successfully!
============================================
访问地址: http://localhost:9099
接口文档: http://localhost:9099/doc.html
Druid监控: http://localhost:9099/druid/
============================================
```

---

**修复时间**: 2025-11-05 19:05  
**状态**: ✅ 已修复，等待用户重启验证

